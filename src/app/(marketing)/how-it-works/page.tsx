import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "How it works \u2014 zybble",
  description: "From keyword + city to a clean, verified lead list in minutes. See exactly how zybble finds, enriches and delivers business leads.",
};

export { default } from "@/views/HowItWorksPage";
