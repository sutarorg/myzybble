import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Company \u2014 zybble",
  description: "Who builds zybble, why we build it, and how we think about business data.",
};

export { default } from "@/views/Company";
