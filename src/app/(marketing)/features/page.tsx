import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Features \u2014 zybble",
  description: "Search Google Maps at scale, verify emails live, capture direct dials, and export clean CSVs. Every feature included in every zybble plan.",
};

export { default } from "@/views/FeaturesPage";
