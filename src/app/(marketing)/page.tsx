import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "zybble \u2014 Turn Google Maps into verified leads",
  description: "zybble finds businesses on Google Maps, verifies their emails & phone numbers, and runs AI-powered outreach campaigns. Export clean CSVs from $19/mo.",
};

export { default } from "@/views/Landing";
